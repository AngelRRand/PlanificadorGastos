import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        marginTop:70,
    },
    title:{
        fontSize:30,
        textAlign:'center',
        fontWeight:700,
    }
})

export default styles