import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        marginTop:20,
        marginBottom:100,
    },
    title:{
        fontSize:30,
        textAlign:'center',
        fontWeight:700,
        marginTop:20
    },
    TextNotExp:{
        marginVertical:20,
        textAlign:'center',
        fontSize:20
    }
})

export default styles